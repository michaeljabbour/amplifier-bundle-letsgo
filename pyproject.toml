# Test and lint configuration only — this is NOT an installable Python package.
# The bundle is composed via bundle.md; modules have their own pyproject.toml.

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "C4"]
