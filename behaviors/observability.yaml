bundle:
  name: behavior-observability
  version: 1.1.0
  description: Telemetry, metrics collection, and session health monitoring

hooks:
  - module: hooks-telemetry
    source: ../modules/hooks-telemetry
    # metrics_path resolved via _resolve_base_dir:
    # config.base_dir > LETSGO_HOME env > ~/.letsgo

context:
  include:
    - letsgo:context/observability-awareness.md
